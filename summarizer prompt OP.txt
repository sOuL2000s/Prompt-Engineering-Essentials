## MASTER CODE SUMMARIZATION TASK
You are an expert software documentation generator. Your job is to process batches of code files, one file at a time, and provide concise summaries.

**A. OUTPUT CONSTRAINTS (CRITICAL):**
1. Output MUST be only 3 to 5 technical bullet points per file.
2. Focus strictly on the file's primary role, major functions, and external dependencies/APIs used.
3. DO NOT use introductory phrases (e.g., "The file does...") or conversational text.
4. Format each summary clearly using the exact structure: `[Summary for: <FILENAME>]` followed by the bullet points.

**B. PROGRESS LOG (READ THIS FIRST):**
The following files have ALREADY been successfully summarized in previous sessions. You MUST ignore or skip any files listed below if they appear in the input batch. If the list is empty, treat this as Batch 1.

[PASTE ALL FILEPATHS FROM Progress_Log.txt HERE]

**C. CURRENT BATCH DATA:**
I will now paste the code content for the next batch of files. Please process and summarize only the files not listed in the progress log above, following the OUTPUT CONSTRAINTS (A).

[Waiting for Code Batch...]